{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://hunt-design-lab.local/schemas/equipment.schema.json",
  "title": "Equipment catalog document",
  "description": "Shape of presets/<mode>/equipment.json. Loaded by presets.loadEquipmentPreset / getEquipmentItem. Storage: catalog document (items[]).",
  "type": "object",
  "required": ["items"],
  "additionalProperties": true,
  "properties": {
    "version": { "type": "integer", "minimum": 1 },
    "notes": { "type": "string", "maxLength": 4000 },
    "items": {
      "type": "array",
      "minItems": 0,
      "items": { "$ref": "#/definitions/equipmentItem" }
    }
  },
  "definitions": {
    "entityId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]{0,79}$",
      "minLength": 1,
      "maxLength": 80
    },
    "equipmentItem": {
      "type": "object",
      "required": ["id"],
      "additionalProperties": true,
      "properties": {
        "id": { "$ref": "#/definitions/entityId" },
        "label": { "type": "string", "minLength": 1, "maxLength": 120 },
        "customSpriteGenre": {
          "type": "string",
          "description": "Optional catalog genre for Custom Sprite when different from the mode genre (e.g. steampunk while editing a standard-mode item). Empty = mode genre. Controls the catalog list and wiki/runtime sprite path for customSprite."
        },
        "customSprite": {
          "type": "string",
          "format": "equipment_sprite_id",
          "options": {
            "assetKind": "equipment",
            "previewVariant": "alpha",
            "selectLabel": "Browse equipment art"
          },
          "description": "Optional genre equipment catalog id used for art when this combat item has no matching sprite under its own id. Designer: Select opens the equipment art catalog filtered by customSpriteGenre (or mode genre). Alpha thumb preview in wiki and form."
        },
        "slot": {
          "type": "string",
          "default": "",
          "enum": [
            "",
            "rightHand",
            "leftHand",
            "helmet",
            "armor",
            "legs",
            "boots",
            "amulet",
            "ring",
            "backpack",
            "ammunition"
          ],
          "description": "Equipment slot. Relation filters on player profiles use these strings. Empty = unset (omit on disk). Potions / runes / currency usually have no slot."
        },
        "category": {
          "type": "string",
          "enum": [
            "sword",
            "axe",
            "club",
            "spear",
            "bow",
            "crossbow",
            "wand",
            "fist",
            "shield",
            "spellbook",
            "helmet",
            "armor",
            "legs",
            "boots",
            "ring",
            "amulet",
            "ammo",
            "quiver",
            "light",
            "container",
            "potion",
            "rune",
            "currency",
            "gem",
            "food",
            "tool"
          ],
          "description": "Item family. Combat + loot-only (currency / gem / food / potion / rune / tool). tool is backpack-only; not a hand slot."
        },
        "weaponType": {
          "type": "string",
          "default": "",
          "enum": ["", "melee", "distance", "magic", "shield"],
          "description": "Weapon family for power curves. Empty = unset (omit on disk). Armor / potions / currency must not invent melee."
        },
        "type": {
          "type": "array",
          "description": "Pipeline type tags (potion, consumable, container, …). Distinct from category.",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "ammunition",
              "amulet",
              "armor",
              "axe",
              "boots",
              "bow",
              "club",
              "consumable",
              "container",
              "crossbow",
              "currency",
              "fist",
              "food",
              "gem",
              "helmet",
              "legs",
              "light",
              "potion",
              "quiver",
              "ring",
              "rod",
              "rune",
              "shield",
              "spellbook",
              "stackable",
              "sword",
              "throwing",
              "wand"
            ]
          }
        },
        "vocation": {
          "type": "array",
          "description": "Allowed class vocations (same ids as classes / spells.vocations).",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "guardian",
              "scout",
              "mystic",
              "adept",
              "warden",
              "adventurer"
            ]
          }
        },
        "level": {
          "type": "integer",
          "minimum": 0,
          "description": "Minimum character level to equip or use."
        },
        "heal": {
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "type": "number", "minimum": 0 },
          "description": "On-use HP restore [min, max]. Catalog is source of truth (e.g. small_health_potion [60, 90])."
        },
        "restoreMana": {
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": { "type": "number", "minimum": 0 },
          "description": "On-use mana restore [min, max]. Catalog is source of truth (e.g. mana_potion [75, 125])."
        },
        "worth": {
          "type": "number",
          "minimum": 0,
          "description": "Currency value in gold units (gold_coin = 1, platinum_coin = 100)."
        },
        "volume": {
          "type": "number",
          "minimum": 0,
          "description": "Container capacity (backpack / depot volume)."
        },
        "imbuementSlots": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of imbuement slots on this item."
        },
        "consumable": {
          "type": "boolean",
          "description": "When true, use consumes one stack unit (potions, food)."
        },
        "usable": {
          "type": "boolean",
          "description": "When true, the item can be used from the backpack (potions)."
        },
        "hitChance": {
          "type": "number",
          "description": "Distance-weapon hit-chance modifier added to ammo/throw maxHitChance (e.g. amber_bow +6 → 91 becomes 97). Not an absolute percent on bows. Live values may be outside 0–100."
        },
        "perfectShotDamage": {
          "type": "number",
          "minimum": 0,
          "description": "Bonus damage when the target is at perfectShotRange."
        },
        "perfectShotRange": {
          "type": "integer",
          "minimum": 0,
          "description": "Chebyshev range at which perfectShotDamage applies."
        },
        "twoHanded": {
          "type": "boolean",
          "description": "True if the weapon requires both hands to be wielded (e.g. bows, crossbows, some swords/axes/clubs)."
        },
        "atk": { "type": "number" },
        "extraAtk": {
          "type": "number",
          "description": "Second attack value on dual-element weapons (fire sword extra 11). Combined with atk for the auto formula, then split by extraAtk/(atk+extraAtk). Physical share uses armor+block; extra share uses extraAtkElement resist only."
        },
        "extraAtkElement": {
          "type": "string",
          "default": "",
          "enum": ["", "physical", "fire", "ice", "energy", "earth", "holy", "death"],
          "description": "Element of extraAtk on dual-element weapons. Required with extraAtk for the split. Empty = unset (omit on disk)."
        },
        "defense": { "type": "number" },
        "defenseBonus": {
          "type": "number",
          "description": "Weapon extradef / defense modifier. Applied to total defense only when a shield contributes defense > 0."
        },
        "armor": { "type": "number" },
        "speed": { "type": "number" },
        "weight": { "type": "number", "minimum": 0 },
        "stackable": {
          "type": "boolean",
          "description": "When true, identical itemId instances merge into one container slot with count (weight = unit × count). Max stack size is 100. Used for arrows/bolts, throwing weapons (spears/stars), and runes (category rune). Omit when false."
        },
        "breakChance": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Throwing weapons only: percent chance (0–100) to destroy one stack unit on each throw (hit or miss). 100 = always consume one. Cap weight uses unit × remaining count."
        },
        "ammoType": {
          "type": "string",
          "default": "",
          "enum": ["", "arrow", "bolt"],
          "description": "Required for category ammo: which distance weapon may fire this stack. Bows consume arrow; crossbows consume bolt. Empty = unset (omit on disk)."
        },
        "autoShape": {
          "type": "object",
          "description": "When distance_auto spends this ammo, resolve as a shaped auto centered on the sticky target. Crit is multiply-only; one crit roll per swing. Omit on ST ammo.",
          "properties": {
            "type": { "type": "string", "enum": ["area"] },
            "code": { "type": "number" }
          }
        },
        "tier": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10,
          "description": "Weapon classification rank. Fatal chance = 0.05 t² + 0.4 t + 0.05 percent when t > 0. Omit or 0 = never. No standard rows ship a tier."
        },
        "range": { "type": "number", "minimum": 0 },
        "min": {
          "type": "number",
          "minimum": 0,
          "description": "Wand/rod auto-attack damage floor (legacy fromDamage). Omit on other items."
        },
        "max": {
          "type": "number",
          "minimum": 0,
          "description": "Wand/rod auto-attack damage ceiling (legacy toDamage). Omit on other items."
        },
        "element": {
          "type": "string",
          "default": "",
          "enum": ["", "physical", "fire", "ice", "energy", "earth", "holy", "death"],
          "description": "Wand/rod auto-attack element (legacy wandType). Empty = unset (omit on disk)."
        },
        "manaGain": {
          "type": "integer",
          "minimum": 0,
          "description": "Mana restored on a wand/rod auto that deals final damage > 0 after reductions. Authored from wand level; not computed at runtime."
        },
        "durationSec": {
          "type": "integer",
          "minimum": 0,
          "description": "Temporary item lifetime in seconds (data-only until tick/decay stage)."
        },
        "charges": { "type": "integer", "minimum": 0 },
        "skillBonuses": { "type": "object", "additionalProperties": { "type": "number" } },
        "resists": { "type": "object", "additionalProperties": { "type": "number" } },
        "regen": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "hp": { "type": "number" },
            "mp": { "type": "number" },
            "hpTicksMs": { "type": ["number", "null"] },
            "mpTicksMs": { "type": ["number", "null"] }
          }
        },
        "flags": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "manaShield": { "type": "boolean" },
            "invisible": { "type": "boolean" }
          }
        },
        "notes": { "type": "string", "maxLength": 2000 },
        "spriteId": {
          "type": "string",
          "description": "Alias of customSprite (equipment picker historically reads spriteId)."
        },
        "maxHitChance": {
          "type": "number",
          "description": "Ammo or throwing-weapon hit-chance cap (percent). Distance auto rolls min(100, maxHitChance + equipped weapon hitChance). Live arrows/bolts are ~76–100. Omit when not ammo/throw."
        },
        "critChance": {
          "type": "number",
          "description": "Additive crit chance. Catalog pipeline units: 1000 = +10%. Stacks with class/profile percent. Runtime roll is 0–100. Omit when none."
        },
        "critExtraDamage": {
          "type": "number",
          "description": "Additive extra crit damage. Catalog pipeline units: 3500 = +35%. Stacks with class/profile critDamage percent. Omit when none."
        },
        "lifeLeechChance": {
          "type": "number",
          "description": "Life-leech proc chance 0–100 (100 = always). Additive across equipped items. Omit when none."
        },
        "lifeLeechAmount": {
          "type": "number",
          "description": "Life-leech amount. Catalog pipeline units: 1800 = 18% of real HP damage on proc. Additive. Omit when none."
        },
        "manaLeechChance": {
          "type": "number",
          "description": "Mana-leech proc chance 0–100 (100 = always). Additive across equipped items. Omit when none."
        },
        "manaLeechAmount": {
          "type": "number",
          "description": "Mana-leech amount. Catalog pipeline units: 300 = 3% of real HP damage on proc. Additive. Omit when none."
        },
        "aliases": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Alternate ids that resolve to this row."
        },
        "dispel": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Condition kinds this item removes on use. Catalog is source of truth (e.g. poison on antidote_potion)."
        },
        "condition": {
          "type": "object",
          "additionalProperties": true,
          "description": "On-use condition applied via applyCondition. Catalog is source of truth (e.g. magic_shield_potion mana_shield). Same shape as spell.condition.",
          "properties": {
            "type": {
              "type": "string",
              "default": "",
              "enum": [
                "",
                "poison",
                "fire",
                "ice",
                "energy",
                "bleed",
                "curse",
                "holy",
                "slow",
                "haste",
                "invisible",
                "regen",
                "attributes",
                "mana_shield"
              ]
            },
            "durationSec": { "type": "number", "minimum": 0 },
            "poolFormula": {
              "type": "string",
              "default": "",
              "enum": ["", "legacy_mana_shield"]
            }
          }
        }
      }
    }
  }
}
